# Hidden Test Readiness

No source-code change is needed to ingest a new PDF/DOCX, a policy version, a role requirement, or an employee. Use the API/UI workflows to upload, review source chunks, add matrix rows, create the employee, generate a plan, and validate it.

The application rejects unsupported source versions, quarantines common prompt-injection patterns, and identifies plan impact from changed document IDs.
